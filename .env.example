# Copy to .env.local and fill values for local development
OPENAI_API_KEY=
# Set to 1 to force file-based memory (no native sqlite required)
FORCE_FILE_MEMORY=1
# Optional Vercel deploy variables
VERCEL_TOKEN=
VERCEL_ORG_ID=
VERCEL_PROJECT_ID=
